import React from 'react';
import { Flex, Text, VStack, HStack, Box, Image } from 'native-base';
import { colors } from 'utils/theme';
import { ITokenPackage } from 'interfaces/token-package';
import NumberFormat from 'components/uis/NumberFormat';
import Button from 'components/uis/Button';
import styles from './style'


const ModelPackageCard = () => (
  <Box
    py={4}
    px={5}
    mb={5}
    h={220}
  >




  </Box>
);

export default ModelPackageCard;
